export enum SubscriptionStatus {
  CREATED = 'CREATED',
  ENABLED = 'ENABLED',
  PAUSED = 'PAUSED',
  CANCELLED = 'CANCELLED',
}

export enum SubscriptionTopic {
  NFTTransfer = 'NFT',
  TOKENTransfer = 'TOKEN',
}
